# =====================================================
# AI ImageGen - Environment Variables
# =====================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!
# =====================================================

# =====================================================
# SUPABASE CONFIGURATION
# =====================================================
# Get these from your Supabase project settings
# Dashboard -> Settings -> API

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon/public key (safe to expose)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Your Supabase service role key (KEEP SECRET - server-side only)
# This is used for admin operations that bypass RLS
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =====================================================
# RAZORPAY CONFIGURATION
# =====================================================
# Get these from your Razorpay dashboard
# Dashboard -> Settings -> API Keys

# Razorpay Key ID (starts with rzp_test_ or rzp_live_)
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx

# Razorpay Key Secret (KEEP SECRET - server-side only)
RAZORPAY_KEY_SECRET=your-razorpay-key-secret

# Razorpay Key ID for client-side (same as RAZORPAY_KEY_ID)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx

# Payment stage: "sandbox" for testing, "production" for live
PAYMENT_STAGE=sandbox

# Razorpay Webhook Secret (optional, for enhanced security)
# Get this from Razorpay Dashboard -> Webhooks
RAZORPAY_WEBHOOK_SECRET=your-webhook-secret

# =====================================================
# GOOGLE CLOUD STORAGE (for image uploads)
# =====================================================
# Get these from Google Cloud Console

# GCS bucket name
GCS_BUCKET_NAME=your-gcs-bucket-name

# Path to service account JSON key file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# =====================================================
# APPLICATION SETTINGS
# =====================================================

# Your application URL (used for OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# Credits per image generation
CREDITS_PER_GENERATION=5

# Default credits for new users
DEFAULT_CREDITS=100

# =====================================================
# OPTIONAL: RATE LIMITING
# =====================================================

# Maximum requests per rate limit window
# RATE_LIMIT_MAX=100

# Rate limit window in milliseconds (default: 15 minutes)
# RATE_LIMIT_WINDOW=900000

# =====================================================
# SECURITY CONFIGURATION
# =====================================================

# CSRF protection secret (auto-generated if not set)
# CSRF_SECRET=your-csrf-secret-key

# API proxy secret for request signing (auto-generated if not set)
# PROXY_SECRET=your-proxy-secret-key

# =====================================================
# SETUP INSTRUCTIONS
# =====================================================
#
# 1. SUPABASE SETUP:
#    - Create a new project at https://supabase.com
#    - Go to Authentication -> Providers -> Google
#    - Enable Google OAuth and add your Google credentials
#    - Set the callback URL to: https://your-project.supabase.co/auth/v1/callback
#    - Run the SQL from supabase-schema.sql in the SQL editor
#
# 2. RAZORPAY SETUP:
#    - Create an account at https://razorpay.com
#    - Go to Settings -> API Keys
#    - Generate test keys for sandbox mode
#    - For production, get live keys and set PAYMENT_STAGE=production
#    - Set up webhooks pointing to: https://your-domain.com/api/payments/webhook
#
# 3. GOOGLE CLOUD STORAGE SETUP:
#    - Create a GCS bucket
#    - Create a service account with Storage Object Admin role
#    - Download the JSON key file
#    - Set CORS policy on the bucket if needed
#
# =====================================================
